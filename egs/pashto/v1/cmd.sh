# -- When running on a local machine ------------------------------------------
export cmd="retry.pl run.pl --max-jobs-run 2"

# -- When running on a GRID ---------------------------------------------------
#export cmd="retry.pl queue.pl --mem 4G"
